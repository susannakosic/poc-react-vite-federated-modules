{
  "name": "vite-federation-react",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "serve": "yarn workspaces run preview",
    "stop": "kill-port --port 5000,5001"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.1.13",
    "kill-port": "^2.0.1"
  }
}
