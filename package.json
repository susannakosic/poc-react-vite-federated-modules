{
  "name": "vite-federation-react",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.0.0"
  },
  "scripts": {
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build",
    "serve": "cd ./packages/app && yarn preview & cd ./packages/shared && yarn preview",
    "stop": "kill-port --port 5000,5001"
  },
  "devDependencies": {
    "@originjs/vite-plugin-federation": "^1.1.14",
    "dotenv": "^16.0.3",
    "kill-port": "^2.0.1"
  }
}
